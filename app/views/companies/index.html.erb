<h1>Компании</h1>

<table class="allorderlist">
  <tr>
    <th>Название</th>
    <th>Сотрудники табелируются?</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @companies.each do |company| %>
  <tr>
    <td><%= company.name %></td>
    <td><%= company.istabelling %></td>
    <td><%= link_to 'Show', company %></td>
    <td><%= link_to 'Edit', edit_company_path(company) %></td>
    <td><%= link_to 'Destroy', company, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Добавить компанию', new_company_path %>
<br />
<%= link_to 'Вернуться в список заказов (на главную)', aorders_path   %>